const About = () => {
    return (
        <div>
            we can create the /about page with two methods : 
            <ul>
                <li>
                     - The first : this is an about page with the (about/index.js) and inside the folder we can added the index page
                </li>
                <li>
                     - The Second : is to create a page with name (about.js) in the Root folder of pages
                </li>
            </ul>
        </div>
    );
}

export default About;